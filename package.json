{
  "name": "aguata_website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "http://makhafi/aguata.github.io/aguata upload",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "react-phone-input-2": "^2.15.1"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
